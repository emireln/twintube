# Local dev overrides (auto-written by scripts/start-local.ps1)
# This file overrides production .env so local runs use SQLite by default.
DB_TYPE=sqlite
DATABASE_URL=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_SSLMODE=
