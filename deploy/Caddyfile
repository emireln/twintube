{
	email {$ACME_EMAIL}
}

# Main app — frontend + backend + WebSockets
twintube.site, www.twintube.site {
	encode gzip
	reverse_proxy app:8080
}

# Redirect legacy marketing subdomain to main app
home.twintube.site {
	redir https://twintube.site{uri} permanent
}
